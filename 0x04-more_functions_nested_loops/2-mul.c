#include "main.h"
#include <stdio.h>

/**
 * mul - multiples the two intergers
 * @a: is an interger
 * @b: is an interger
 *
 * Return: product
 */

int mul(int a, int b)
{
	int product;

	product = (a * b);
	return (product);
}
